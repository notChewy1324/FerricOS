# greetd is not supported by the displaymanager module, so it is enabled
# here instead (displaymanager is deliberately absent from settings.conf).
---
services:
  - name: greetd
    mandatory: true
  - name: NetworkManager
    mandatory: true
  - name: fstrim.timer
    mandatory: false
