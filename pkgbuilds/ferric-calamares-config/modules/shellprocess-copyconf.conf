# Persist the live pacman.conf ([ferric] + [multilib]) onto the target so
# installed systems keep receiving ferric updates.
---
dontChroot: true
timeout: 30
script:
  - command: "cp /etc/pacman.conf @@ROOT@@/etc/pacman.conf"
